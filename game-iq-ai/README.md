# Game IQ AI — Prototype

Mobile-first web app prototype for a premium DTC soccer video-analysis product
aimed at parents, coaches, and players.

## Run it

No build step. Open `index.html` in a browser, or serve the folder:

```
cd game-iq-ai
python3 -m http.server 8000
```

Then open http://localhost:8000 — use your browser's mobile viewport
(390x844) for the intended experience.

There is also a single-file build, `app.html`, generated by
`python3 build_single.py`. It inlines all screens, styles, and behavior
into one HTML file with hash-based routing — ideal for opening on a phone
from a single URL.

## Screens

| File | Screen |
| --- | --- |
| `index.html` | Landing page (biomechanics positioning) |
| `signup.html` | Sign up + 4-step onboarding (role, player profile incl. 7 positions, goals) |
| `upload.html` | Upload flow (MP4 / YouTube / public URL) |
| `identify.html` | Player identification flow (detected candidates + manual confirm) with simulated analysis run |
| `analysis.html` | Full analysis report (sample data) |
| `coach.html` | Coach view — tactical depth, tagging, telestration, staff notes |
| `parent.html` | Parent view — plain-English explanation |
| `player.html` | Player view — development plan and weekly missions |
| `progress.html` | Season progress tracker |
| `pricing.html` | Pricing / paywall with advanced-report upsell |

## Analysis engine (mechanics & biomechanics)

The report is structured around six mechanics categories, each scored with a
High / Medium / Low confidence label and never overclaiming what the footage
can't show:

1. **Running** — acceleration, deceleration, stride, balance, change of direction
2. **Kicking** — support foot, hip-knee-ankle sequencing, contact, follow-through
3. **Receiving** — body shape, first-touch angle, control under pressure
4. **Movement science** — braking, re-acceleration, turning, posture in duels
5. **Ball physics** — pass speed, trajectory, spin (where inferable)
6. **Tactical mechanics** — scanning, support angles, spacing, pressing triggers

The analysis page also includes: executive summary, technical/tactical/mechanics
scores, confidence score, on-ball and off-ball sections, movement-science notes,
strengths, improvement areas, a key-moments timeline, tactical notes, coach
action points, and a comparison with the previous upload.

## Product decisions baked in

- **Persona-tailored reports**: every analysis is written three ways —
  parent (plain English), coach (tactical depth), player (direct,
  motivating). This is the core differentiator.
- **Confidence score**: each report states how trustworthy it is based on
  footage length, resolution, and player trackability. Builds trust and
  nudges users toward longer/better footage.
- **Subscription structure** (for the $50/month parent offer):
  - Free first analysis, no card — the report itself is the sales pitch.
  - 7-day free trial on paid plans, 30-day money-back guarantee.
  - **Pro Monthly $50/mo** and **Pro Annual $480/yr ($40/mo, save 20%)** —
    annual is the recommended default since development is season-long.
  - 4 full-match analyses per month (credits roll over 60 days) plus
    unlimited short training clips — predictable COGS, feels unlimited.
  - Off-season pause (annual only) instead of cancellation.
  - Sibling add-on at $15/mo to grow family LTV.

## Design system

- Single shared stylesheet (`styles.css`) with CSS variables; dark and
  light themes (`data-theme` on `<html>`, persisted to localStorage,
  defaults to system preference).
- Card-based layout, 52px minimum tap targets, max-width 480px column.
- No external dependencies, fonts, or images — system font stack and
  inline SVG icons only.

All analysis content is realistic sample data for a fictional U13
midfielder ("Jordan").
